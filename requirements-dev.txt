pytest~=8.3                    # Python testing framework, runs test files and functions
pytest-asyncio~=0.23           # Adds async/await support to pytest tests
httpx~=0.27                    # Modern HTTP client for Python (like requests but async-capable)
pytest-cov~=5.0                # Adds code coverage reporting to pytest
black~=24.8                    # Automatic code formatter (enforces consistent style)
isort~=5.13                    # Sorts and organizes import statements
ruff~=0.6                      # Fast Python linter and formatter (replaces flake8 + black)
pre-commit~=3.7                # Runs checks (formatting, linting) before git commits
ipython~=8.26                  # Enhanced Python REPL with better debugging and features
